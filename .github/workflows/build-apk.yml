name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Cache buildozer
      uses: actions/cache@v5
      with:
        path: |
          .buildozer_cache
        key: docker-buildozer-v2
        restore-keys: docker-buildozer-

    - name: Build APK with Kivy Docker image
      run: |
        mkdir -p .buildozer_cache
        # 预接受Android SDK license
        mkdir -p .buildozer_cache/android/platform/android-sdk/licenses
        echo -e "\n24333f8a63b6825ea9c5514f83c2829b004d1fee" > .buildozer_cache/android/platform/android-sdk/licenses/android-sdk-license
        echo -e "\n84831b9409646a918e30573bab4c9c9f46a98b00" > .buildozer_cache/android/platform/android-sdk/licenses/android-sdk-preview-license
        
        docker run --rm \
          --entrypoint buildozer \
          -v "$PWD":/home/user/project \
          -v "$PWD/.buildozer_cache":/root/.buildozer \
          -w /home/user/project \
          kivy/buildozer:latest \
          android debug \
          2>&1 | tee build_output.log

    - name: Extract errors
      if: always()
      run: |
        echo "=== ERRORS ==="
        grep -iE "error|fail|traceback|exception|command failed" build_output.log | grep -v "DEBUG\|INFO\|warning\|deprecated\|DeprecationWarning\|stty" | tail -50
        echo ""
        echo "=== LAST 50 LINES ==="
        tail -50 build_output.log

    - name: Find APK
      if: always()
      run: |
        echo "=== Searching for APK ==="
        find . -name "*.apk" -ls 2>/dev/null || echo "No APK found"
        ls -la bin/ 2>/dev/null || echo "no bin/"

    - name: Upload build log
      if: always()
      uses: actions/upload-artifact@v5
      with:
        name: build-log
        path: build_output.log
        retention-days: 7

    - name: Upload APK
      uses: actions/upload-artifact@v5
      if: success()
      with:
        name: pharmacy-apk
        path: bin/*.apk
        if-no-files-found: ignore
        retention-days: 30

    - name: Release APK
      uses: softprops/action-gh-release@v2
      if: success()
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: 药房比价系统 v1.0.${{ github.run_number }}
        body: "APK build #${{ github.run_number }}"
        files: bin/*.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
